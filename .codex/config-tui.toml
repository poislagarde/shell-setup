# Codex TUI settings — merged into ~/.codex/config.toml by /shell-setup §15.
# ~/.codex/config.toml is otherwise machine-local state (project trust levels,
# marketplaces, hook trust hashes): never copy it wholesale — append/reconcile
# this block only.

[tui]
status_line = [
  "context-used",
  "model-with-reasoning",
  "pull-request-number",
  "weekly-limit",
  "five-hour-limit",
]
